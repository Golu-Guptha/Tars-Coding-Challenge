const authConfig = {
    providers: [
        {
            domain: "https://sweet-pangolin-57.clerk.accounts.dev",
            applicationID: "convex",
        },
    ],
};

export default authConfig;
